//1. function expression called divide
const divide = function () {
  return 2000 / 100;
};

//2. arrow function called square
const square = (root) => root * root;

//3. arrow function called add
const add = (x, y) => x + y;
